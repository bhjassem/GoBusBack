# ===========================================
# GoBus Backend - Environment Variables
# ===========================================
# Copier ce fichier en .env et adapter les valeurs.
# Le fichier .env ne doit JAMAIS être commité.
# ===========================================

# SSL Verification for internal OAuth calls
# Set to "false" for local development (no SSL certificate)
# In production: remove this line or set to "true"
GOBUS_SSL_VERIFY=false

# OAuth Client Credentials (Simple OAuth / Drupal)
# Must match the Consumer entity created in Drupal admin
OAUTH_CLIENT_ID=your-client-id-here
OAUTH_CLIENT_SECRET=your-client-secret-here

# Drupal Hash Salt
# Generate with: php -r "echo bin2hex(random_bytes(55));"
DRUPAL_HASH_SALT=generate-a-new-hash-salt-here
